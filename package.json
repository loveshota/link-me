{
  "name": "link-me",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy",
    "gen-2fa": "node scripts/gen-2fa.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
