{
  "name": "link-me",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "cloudflare": {
    "bindings": {
      "CONTACTS_KV": {
        "description": "在 KV 中存储管理员凭据与联系方式，部署时自动创建，无需手动填写。"
      }
    }
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "qrcode-generator": "^2.0.4"
  }
}
