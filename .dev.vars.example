# 本项目所有配置都有合理默认值，一键部署无需手动填写任何内容。
#
# 以下均为可选项，仅在确有需要时，通过 Cloudflare 控制台
# Settings → Variables and Secrets（或 `npx wrangler secret put`）添加：
#
# SETUP_TOKEN     初始化口令。设置后首次初始化必须填写相同口令，
#                 防止他人抢先占用管理员；不设置则不校验。
# SESSION_SECRET  自定义会话签名密钥（64 位十六进制）；
#                 不设置则初始化时自动生成随机密钥。
